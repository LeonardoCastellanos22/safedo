[Unit]
Description=Flask Application
After=network.target

[Service]
User=pi
Group=pi
WorkingDirectory={{ app_dir }}
Environment="PATH={{ venv_path }}/bin"
ExecStart={{ venv_path }}/bin/python {{ app_file }}
Restart=always

[Install]
WantedBy=multi-user.target
